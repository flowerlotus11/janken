const GU = 0;

let matches = 0;

function action(oppornent){
  matches = matches + 1;
  const amari = matches % 2;
  const isEven = amari == 0;
  console.log(`${matches}) % 2 = ${reminder} : ${isEven}`);
  if(isEven){
   return GU;
  }
  return CHOKI;
}
